# sysenv
CLI utility to show linux environment info:
-distro
-init system
-bootloader guess
-root status
-systemd availability

# usage
run:
sysenv

#install
make executable:
chmod +x sysenv

copy to system path:
sudo cp sysenv /usr/local/bin/sysenv
